{
  "name": "rbk-forum-collector",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension — collect XSMB picks from forumketqua.net",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
